{
  "name": "@saga-bus/example-redis",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Redis-based saga example using Redis Streams transport and Redis store",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsc",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@saga-bus/core": "workspace:*",
    "@saga-bus/examples-shared": "workspace:*",
    "@saga-bus/transport-redis": "workspace:*",
    "@saga-bus/store-redis": "workspace:*",
    "@saga-bus/middleware-logging": "workspace:*",
    "@saga-bus/middleware-idempotency": "workspace:*",
    "ioredis": "^5.6.1"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^20.0.0",
    "tsx": "^4.19.4",
    "typescript": "^5.9.2"
  }
}
